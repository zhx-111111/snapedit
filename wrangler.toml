# Cloudflare Pages 配置
# 文档：https://developers.cloudflare.com/pages/platform/wrangler

name = "snapedit"
compatibility_date = "2026-08-14"

# 纯静态站点，无需构建
# Cloudflare Pages 会自动将项目根目录作为发布目录

# 环境变量（如需扩展可在此添加）
# [vars]
# APP_ENV = "production"
